spring:
  application:
    name: triage-consumer
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: triage-agent-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

triage:
  agent:
    base-url: ${TRIAGE_AGENT_BASE_URL:http://localhost:8000}
    # fail-open: if the Python service is down, log and skip rather than
    # blocking the consumer / retrying forever and stalling the partition
    timeout-ms: 15000
    escalate-on-error: true

server:
  port: 8080
